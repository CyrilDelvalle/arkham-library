import styled from "styled-components";

const InvestigatorCardWrapper = styled.div`
    transition: background 200ms ease-in, color 200ms ease-in

    .card-wrapper {
    } 

    &:hover {
        transform:scale(1.02)   
    }
`;

export default InvestigatorCardWrapper;
